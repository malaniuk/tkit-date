export const S = 1000;
export const M = S * 60;
export const H = M * 60;
export const D = H * 24;

export const s = (value: number) => value * S;
export const m = (value: number) => value * M;
export const h = (value: number) => value * H;
export const d = (value: number) => value * D;
